#pragma once

#include <stdbool.h>

//checks the validity of the createList function, which creates an empty list
bool testCreateList();

//checks the validity of the isEmpty function, which determines whether the list is empty
bool testIsEmpty();

//checks the validity of the addContact function that adds a contact to the list
bool testAddContact();