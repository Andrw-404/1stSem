#pragma once

#include <stdbool.h>

//test that checks the operability of the haveBrackets function
bool testForHaveBrackets();

//test that checks the operability of the advancedBracketBalance function
bool testForBalance();
