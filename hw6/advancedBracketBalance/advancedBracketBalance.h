#pragma once

//function that checks whether brackets are in balance or not
bool advancedBracketBalance(char* string);

//function that checks for the presence of brackets in a string
bool haveBrackets(char* string);
