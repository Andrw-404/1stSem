#include "list.h"
#include "stdio.h"

int main(void) {
    List* list = createList();
    addLast(list, 1);
    addLast(list, 2);
    addLast(list, 3);
    addLast(list, 4);
    addLast(list, 5);

    remov

}