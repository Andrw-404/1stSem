#pragma once

#include <stdbool.h>

// test for adding a node to a tree
bool testForAdd();

// test to find node in tree
bool testForSearch();

// test to remove a node from a tree
bool deleteNodeTest();

// function to run tests
bool runTests();